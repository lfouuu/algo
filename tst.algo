FUNCTION Produitscalaire(o,p) :: ARRAY_OF INTEGER[]):BOOLEAN
VAR
    sum:  INTEGER:=0;
BEGIN

    FOR I FROM 1 TO o. lenth STEP DO

    sum:= sum+O+[I]*p[I]
   END FOR  
IF (sum = 0) THEN
  RETURN TRUE

  ELSE 
    RETURN FALSE 
END_IF
   END     




